<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Herencia del formulario de res.partner -->
    <record id="view_res_partner_form_whatsapp" model="ir.ui.view">
        <field name="name">res.partner.form.whatsapp</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Stat button de WhatsApp en la barra de botones -->
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <button name="action_view_whatsapp_messages"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-whatsapp"
                        groups="uniasser_whatsapp.group_whatsapp_user">
                    <field name="whatsapp_message_count"
                           widget="statinfo"
                           string="WhatsApp"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
